extends ./layouts/main.pug
include ./mixins/videoBlock

block contents
    .home-page
        .videos
            each video in videos
                +videoBlock({
                    id:video._id,
                    videoFile: video.videoFile,
                    title: video.title,
                    view: video.view
                })
