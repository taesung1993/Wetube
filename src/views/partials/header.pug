header.header
    .header__column
        a(href="/")
            span#logo
                i.fab.fa-youtube
            span#logoname WETUBE
    .header__column
        include ./searchForm
    .header__column
        //- 로그인 됐을 때
        if loggedUser !== null
            a(href=`${routes.upload}`)
                div.icon
                    i.fas.fa-video
                    span.meaning UPLOAD
            a(href=`/user${routes.me}`)
                div.icon
                    i.fas.fa-user-check
                    span.meaning MY
            a(href=`${routes.logout}`)
                div.icon
                    i.fas.fa-unlock
                    span.meaning LOGOUT
        //- 로그인 안됐을 때
        else
            a(href=`${routes.login}`)
                div.icon
                    i.fas.fa-lock
                    span.meaning LOGIN
            a(href=`${routes.join}`)
                div.icon
                    i.fas.fa-user-plus
                    span.meaning JOIN
        div#SideHeaderOpen.icon
            .hamburger__line
            .hamburger__line
            .hamburger__line
.header__menu
    .header__menu__title
        #SideHeaderClose.hamburger
            .hamburger__line
            .hamburger__line
            .hamburger__line
        h3 Menu
    ul.menu-list
        if loggedUser !== null
            a(href=`${routes.upload}`) 🎥 UPLOAD
            a(href=`/user${routes.me}`) 😊 MY
            a(href=`${routes.logout}`) 🖐 LOGOUT
        else
            a(href=`${routes.login}`) 🔒 LOGIN
            a(href=`${routes.join}`) 👨‍👩‍👧‍👦 JOIN
